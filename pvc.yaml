apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-pvc
  namespace: tp-app
spec:
  accessModes:
    - ReadWriteMany     
  storageClassName: azurefile 
  resources:
    requests:
      storage: 1Gi       